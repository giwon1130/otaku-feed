# 로드맵

> **누가 봄**: PM, 신규 합류자, 외부 (이해관계자)
> **언제 봄**: 다음 분기 계획, 변경 이력 확인
> **함께 봐야 할 문서**: [`요구사항.md`](요구사항.md), [`엔지니어링/의사결정로그.md`](../엔지니어링/의사결정로그.md)

## 단기 (다음 1–2주)

### 백엔드 통합 테스트
- `@SpringBootTest`로 `/swipes/bulk` 등 controller 레벨 테스트
- TestContainers Postgres로 DB까지 검증

### Postgres EXPLAIN 인덱스 효과 측정
- 실 데이터 쌓이면 V3 인덱스가 효과 보는지 확인
- 안 쓰는 단순 인덱스(idx_swipes_user_id 등) drop

### EAS 빌드 도입
- 7일 만료 회피 — 1년 .ipa
- Apple Developer 계정 ($99/년) 결제 후 진행

### Sentry 실 설치
- 다음 풀빌드 시 함께 (네이티브 모듈)
- `src/utils/logger.ts`만 1파일 교체

## 중기 (1–2개월)

### 알림 (expo-notifications)
- 이미 패키지는 설치됨 (kakao SDK가 native에서 요구해서)
- 기능적 사용: 새 시즌 신작 알림, 시리즈 신작 알림, 친구 좋아요(소셜 도입 시)

### 시청 진행 추적
- 디테일에 "1화 봤어" 체크 → 진행률 시각화
- 백엔드에 `episode_progress` 테이블 추가

### 디테일 화면 개선
- 캐릭터 정보 (AniList characters)
- 성우 정보
- 평점 분포 (히스토그램)

### 카피 다듬기
- 빈 상태 메시지 더 친근하게
- 에러 메시지 톤 맞춤

## 장기 (3개월+)

### 다국어 (i18n)
- 일본어 우선 (애니 메인 시장)
- 영어 (글로벌 사용자 대비)
- `src/i18n/strings.ts` 구조는 이미 다국어 대비

### 위젯
- iOS 홈 화면 위젯: "오늘의 픽" 1장
- Android 위젯

### 소셜 (선택적)
- 친구 좋아요 공유 (현재 Share 시트 → 메시지 위주)
- 추천 비교 ("우리 둘 다 좋아한 작품")
- 비-목표였지만 사용자 요구 시 재검토

## 변경 이력 (역순, 최근부터)

> 이 섹션은 정기적으로 정리. 자세한 코드 변경은 git log + AGENTS.md 참고.

### 2026-04
- **백엔드 종료**: Railway 비용 문제로 백엔드 서비스 종료, 클라이언트는 로컬-only 모드로 전환 (ADR-015)
- **App Store 제출 가이드 + TestFlight + 개인정보 처리방침** 문서 셋업 (Apple Developer 유료 가입 직후 — 현재 보류)
- **EAS Build 셋업** — eas.json 프로필 + scripts (백엔드 종료 전 작업, 향후 사용 가능)
- **성능 라운드 4**: HomeTab trending+seasonal 단일 쿼리 + SWR raw English + render-then-translate
- **성능 라운드 3**: ANIME_FIELDS 분리(슬림/풀) + render-then-translate + fetchAnimeById 2-tier 캐시
- **백엔드 비용 절감**: HikariCP + lazy init + V3 정렬 인덱스 + JWT 401 fix
- **백엔드 1차**: gzip + /swipes/bulk + 응답 슬림화 (id 제거)
- **모바일 비용 절감**: keepalive 조건부 + apiMe 캐시 + 5분 TTL + swipe 배치 푸시
- **백엔드 헬스 배지** + msw로 AniList client 통합 테스트
- **번역 배치 + AniList SWR + 동시성 + Railway keepalive + DeepL 쿼터** + logger + GitHub Actions CI
- **JWT를 expo-secure-store**(keychain)로 + expo-notifications 추가
- **LoadingBar + 라프텔 한국 출시명** 보강 + 번역 캐시 무효화
- **AniList synonyms에서 한국 출시명 우선** ("장송의 프리렌")
- **공용 컴포넌트 + 모듈 분해 + AniListError + Toast + 메모리 캐시 + i18n + node:test**
- 시리즈 그룹 디바이더 + 멀티 anchor 캐러셀 + 취향 재분석 즉시 갱신
- 시리즈 관계 필터 확장 (SPIN_OFF/ALTERNATIVE/SUMMARY)
- 디테일 줄거리 잘림 fix + 시리즈 보는 순서 + 취향 분석 온보딩
- 라프텔 통합 + ExternalLink regional + ExploreTab 장르 필터 + MyListTab 인사이트
- 5개 사용성 업그레이드 (홈 추천 / 검색 한글 / 비슷한 작품 / 정렬·검색 / 자동 prefetch)

### 2026-03 이전
- OAuth 도입 (카카오/구글)
- 백엔드 Railway 배포
- 초기 구현
